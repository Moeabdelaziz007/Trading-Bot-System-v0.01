{
  "name": "axiom-learning-loop",
  "version": "1.0.0",
  "description": "AXIOM Self-Learning Data Loop System for Trading",
  "main": "index.ts",
  "scripts": {
    "deploy:all": "npm run deploy:db && npm run deploy:outcome && npm run deploy:metrics && npm run deploy:weights && npm run deploy:api",
    "deploy:db": "cd outcome-tracker && wrangler deploy",
    "deploy:outcome": "cd outcome-tracker && wrangler deploy",
    "deploy:metrics": "cd metrics-aggregator && wrangler deploy",
    "deploy:weights": "cd weight-optimizer && wrangler deploy",
    "deploy:api": "cd api-endpoints && wrangler deploy",
    "test": "cd tests && ts-node integration-test.ts",
    "dev": "wrangler dev",
    "format": "prettier --write .",
    "lint": "eslint . --ext .ts,.js"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241203.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.60.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.25",
    "eslint": "^8.56.0",
    "prettier": "^3.1.1",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "AXIOM Trading System",
  "license": "MIT",
  "keywords": [
    "trading",
    "machine-learning",
    "cloudflare",
    "d1",
    "kv",
    "cron",
    "api"
  ]
}
